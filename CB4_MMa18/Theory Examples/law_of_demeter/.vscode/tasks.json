{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/law_of_demeter.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}